{
  "name": "sneaker-shop-backend",
  "version": "1.0.0",
  "description": "Backend for Air Jordan 1 Sneaker Shop",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seeders/seed.js",
    "test": "jest --coverage",
    "test:ci": "jest --coverage --coverageReporters=text --coverageReporters=lcov --ci"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
